fn fib(x: int) -> int {
    if x<=1 {
        return 1;
    }
    let result: int = fib(x - 1);
    result = result + fib(x - 2);
    return result;
}

fn main() -> int {
    let i: int = 0;
    let j: int;
    j = getint();
    while i < j {
        let x: int = 8;
        putint(i);
        putchar(32);
        putint(fib(i));
        putln();
        i = i + 1;//vevevd
    }
    //vevevd
    return 0;
}
//vevevd
